name: Formatar eventos e sessão temporariamente

on:
  push:
    branches:
      - fix/revisao-consistencia-codex
    paths:
      - .github/workflows/format-control-errors-temporary.yml

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: fix/revisao-consistencia-codex
          fetch-depth: 0
      - uses: actions/setup-node@v5
        with:
          node-version: 22.23.1
          cache: npm
      - run: npm ci --ignore-scripts
      - name: Aplicar Prettier canônico
        run: >-
          npx prettier --write
          packages/database/src/control.ts
          packages/database/src/domain-error-codes.spec.ts
      - name: Remover workflow temporário e publicar
        shell: bash
        run: |
          rm -f .github/workflows/format-control-errors-temporary.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add packages/database/src/control.ts packages/database/src/domain-error-codes.spec.ts .github/workflows/format-control-errors-temporary.yml
          git commit -m "style: aplica prettier canônico a eventos e sessão"
          git push origin HEAD:fix/revisao-consistencia-codex
