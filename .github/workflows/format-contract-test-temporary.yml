name: Formatar teste de contrato

on:
  push:
    branches:
      - fix/fluxo-vendas-mesas-despesas
    paths:
      - .github/workflows/format-contract-test-temporary.yml

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: fix/fluxo-vendas-mesas-despesas
          fetch-depth: 0
      - uses: actions/setup-node@v5
        with:
          node-version: '22.23.1'
          cache: npm
      - run: npm ci --ignore-scripts
      - run: npx prettier --write packages/contracts/src/index.spec.ts
      - shell: bash
        run: |
          rm -f .github/workflows/format-contract-test-temporary.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "style: formata teste de contrato" || exit 0
          git push origin HEAD:fix/fluxo-vendas-mesas-despesas
